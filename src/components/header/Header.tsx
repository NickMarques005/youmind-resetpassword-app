import Logo from '../logo/LogoApp';
import './Header.css';

const Header = () => {
    return (
        <header className="header-app">
            <Logo/>
        </header>
    )
}

export default Header;